services:
  - type: web
    name: collabdocs-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server-demo.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
